# Verify Fix Workflow

```text
Verify this fix with fresh evidence.
Run the smallest relevant test, build, lint, or manual reproduction that proves the claim.
Report the actual result, not the expected result.
```
