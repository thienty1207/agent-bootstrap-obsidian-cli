# Test First When Practical

- For bugfixes and behavior changes, prefer a failing test or reproduction first.
- Verify the failure is the expected one before writing implementation code.
- Keep tests focused on one behavior at a time.
