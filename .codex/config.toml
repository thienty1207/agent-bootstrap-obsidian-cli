[agents]
max_threads = 6
max_depth = 1

# Advanced option:
# Set max_threads = 10 only when you intentionally want every shipped custom
# agent available for broad parallel delegation in a large task.
