{
  "name": "agent-bootstrap-obsidian-cli",
  "version": "0.1.0",
  "description": "Portable Obsidian vault bootstrap CLI for coding projects",
  "files": [
    "bin",
    "dist",
    ".github",
    "docs",
    "plans",
    "README.md"
  ],
  "bin": {
    "agent-bootstrap": "./bin/agent-bootstrap.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.9.3"
  }
}
