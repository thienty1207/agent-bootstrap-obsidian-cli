{
  "name": "agent-bootstrap-obsidian-cli",
  "version": "0.1.0",
  "description": "Portable Obsidian vault bootstrap CLI for coding projects",
  "files": [
    ".github",
    "docs",
    "plans",
    "README.md"
  ],
  "bin": {
    "agent-bootstrap": "./.github/agent-bootstrap-runtime/bin/agent-bootstrap.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/sync-runtime.js",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.9.3"
  }
}
