{
  "name": "@tytybill123/agent-bootstrap",
  "version": "0.1.7",
  "description": "Portable Obsidian vault bootstrap CLI for coding projects",
  "files": [
    ".agent",
    "docs",
    "plans",
    "runtime",
    "README.md"
  ],
  "bin": {
    "agent-bootstrap": "runtime/agent-bootstrap/bin/agent-bootstrap.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/sync-runtime.js",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.9.3"
  }
}
